apiVersion: v1
kind: Service
metadata:
  annotations: null
  labels:
    app.kubernetes.io/component: gateway
    app.kubernetes.io/instance: extra-config
    app.kubernetes.io/name: loki
    app.kubernetes.io/version: 3.7.2
    helm.sh/chart: loki-17.4.11
  name: extra-config-loki-gateway-exporter
  namespace: extra-config
spec:
  ports:
    - name: http-metrics
      port: 4040
      protocol: TCP
      targetPort: http-metrics
  selector:
    app.kubernetes.io/component: gateway
    app.kubernetes.io/instance: extra-config
    app.kubernetes.io/name: loki
  type: ClusterIP
