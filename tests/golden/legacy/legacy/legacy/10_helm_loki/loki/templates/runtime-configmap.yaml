apiVersion: v1
data:
  runtime-config.yaml: |
    {}
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/instance: legacy
    app.kubernetes.io/name: loki
    app.kubernetes.io/version: 3.7.8
    helm.sh/chart: loki-18.13.5
  name: legacy-loki-runtime
  namespace: legacy
