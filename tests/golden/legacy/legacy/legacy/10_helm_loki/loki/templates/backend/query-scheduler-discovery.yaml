apiVersion: v1
kind: Service
metadata:
  annotations: null
  labels:
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: legacy
    app.kubernetes.io/name: loki
    prometheus.io/service-monitor: 'false'
  name: legacy-loki-query-scheduler-discovery
  namespace: legacy
spec:
  clusterIP: None
  ports:
    - name: http-metrics
      port: 3100
      protocol: TCP
      targetPort: http-metrics
    - name: grpc
      port: 9095
      protocol: TCP
      targetPort: grpc
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: legacy
    app.kubernetes.io/name: loki
  type: ClusterIP
