apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/component: ingester
    app.kubernetes.io/instance: openshift
    app.kubernetes.io/name: loki
    app.kubernetes.io/version: 3.7.2
    helm.sh/chart: loki-17.4.11
  name: openshift-loki-ingester
  namespace: openshift
spec:
  maxUnavailable: 1
  selector:
    matchExpressions:
      - key: rollout-group
        operator: NotIn
        values:
          - ingester
    matchLabels:
      app.kubernetes.io/component: ingester
      app.kubernetes.io/instance: openshift
      app.kubernetes.io/name: loki
