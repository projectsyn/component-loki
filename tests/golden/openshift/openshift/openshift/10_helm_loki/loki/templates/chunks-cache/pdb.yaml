apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/component: memcached-chunks-cache
    app.kubernetes.io/instance: openshift
    app.kubernetes.io/name: loki
  name: openshift-loki-chunks-cache
  namespace: openshift
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: memcached-chunks-cache
      app.kubernetes.io/instance: openshift
      app.kubernetes.io/name: loki
