apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/component: memcached-results-cache
    app.kubernetes.io/instance: defaults
    app.kubernetes.io/name: loki
  name: defaults-loki-results-cache
  namespace: defaults
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: memcached-results-cache
      app.kubernetes.io/instance: defaults
      app.kubernetes.io/name: loki
