apiVersion: v1
kind: Service
metadata:
  annotations: null
  labels:
    app.kubernetes.io/component: gateway
    app.kubernetes.io/instance: defaults
    app.kubernetes.io/name: loki
    app.kubernetes.io/version: 3.7.4
    helm.sh/chart: loki-18.5.4
  name: defaults-loki-gateway-exporter
  namespace: defaults
spec:
  ports:
    - name: http-metrics
      port: 4040
      protocol: TCP
      targetPort: http-metrics
  selector:
    app.kubernetes.io/component: gateway
    app.kubernetes.io/instance: defaults
    app.kubernetes.io/name: loki
  type: ClusterIP
